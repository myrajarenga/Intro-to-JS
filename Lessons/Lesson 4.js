// we can use a while loop to create a countdown
let num = 10;
while (num >= 0) {
	console.log(num);
	num--;
}

// Here's an array:
const names = [ "Matthew", "James", "Cyrus", "Aditya", "Elizabeth", "Katy" ];

// Can you use a for loop to print each of these names to the console?

for (const name of 
     names) {
      console.log(name);
     }






















































































// Here's a hint if you're stuck:
// A for loop looks like this:
/*
for (initialiser; condition; increment) {
	// code
}
// OR you could use the for of loop
for (initiliser of array) {
	// code
}
*/